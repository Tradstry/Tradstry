package middleware

// Note: Authecroservice receives user_id in the X-User-Id header
// and user_secret in the request body (decrypted by Rust before passing).
// This middleware file is kept for future use if needed.ntication is handled by the Rust backend.
// The Go mi
