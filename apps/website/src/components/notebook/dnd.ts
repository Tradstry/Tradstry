/** Drag payload for a note row. A custom MIME type keeps foreign drags out. */
export const NOTE_DND_TYPE = "application/x-tradstry-note";

/** Drag payload for a folder row, so folders can be nested into one another. */
export const FOLDER_DND_TYPE = "application/x-tradstry-folder";
