// The app-wide scroll area is now the shadcn ScrollArea (thin, themed,
// auto-hiding scrollbar). Re-exported here so existing imports from
// `../user-interface` keep working.
export { ScrollArea, ScrollBar } from "@/components/ui/scroll-area";
